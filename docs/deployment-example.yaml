apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-deployment
  namespace: default
  labels:
    app: nginx
spec:
  replicas: 5
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
      annotations:
        macvlan.pandaria.cattle.io/ip: "auto"
        macvlan.pandaria.cattle.io/subnet: "example-subnet"
        macvlan.pandaria.cattle.io/mac: ""
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-alpine
  namespace: default
  labels:
    app: alpine
spec:
  replicas: 5
  selector:
    matchLabels:
      app: alpine
  template:
    metadata:
      labels:
        app: alpine
      annotations:
        macvlan.pandaria.cattle.io/ip: "auto"
        macvlan.pandaria.cattle.io/subnet: "example-subnet"
        macvlan.pandaria.cattle.io/mac: ""
    spec:
      containers:
      - name: alpine
        image: alpine
        command:
        - "sleep"
        args:
        - "infinity"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-alpine-ipv6
  namespace: default
  labels:
    app: alpine-ipv6
spec:
  replicas: 5
  selector:
    matchLabels:
      app: alpine-ipv6
  template:
    metadata:
      labels:
        app: alpine-ipv6
      annotations:
        macvlan.pandaria.cattle.io/ip: "auto"
        macvlan.pandaria.cattle.io/subnet: "example-subnet-ipv6"
        macvlan.pandaria.cattle.io/mac: ""
    spec:
      containers:
      - name: alpine
        image: alpine
        command:
        - "sleep"
        args:
        - "infinity"
